
var xs : int[];
var ys : int[];
var zs : int[];

invariant foreach p in lhs, q in rhs {
  p = q;
}

invariant foreach y in ys, z in zs {
  y = z;
}

procedure incrX() {
  for l : int in x {
    l = l + 1;
  }
}
  
procedure incrY() {
  for l : int in y {
    l = l + 1;
  }
}
  
procedure incrZ() {
  for l : int in z {
    l = l + 1;
  }
}
